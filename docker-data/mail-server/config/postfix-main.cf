# Override (merge) postfix settings `/etc/postfix/main.cf`.
#
# `[a-z]` で始まる行は `postconf -e` コマンドに渡されたて postfix の設定の上書きに利用される:
# https://github.com/docker-mailserver/docker-mailserver/blob/v10.5.0/target/scripts/startup/setup-stack.sh#L810-L824
#
# See: https://docker-mailserver.github.io/docker-mailserver/v10.5/config/advanced/override-defaults/postfix/

luser_relay = catchall

# 配送設定を可能にする。
# See: https://github.com/docker-mailserver/docker-mailserver/issues/254#issuecomment-521184784
transport_maps = pcre:/tmp/docker-mailserver/postfix-transport_maps
